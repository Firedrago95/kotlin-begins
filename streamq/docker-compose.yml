version: '3.8'

services:
  api:
    build: .
    container_name: streamq-api
    ports:
      - "8080:8080"
    depends_on:
      postgres:
        condition: service_healthy
    environment:
      - SPRING_DATASOURCE_URL=jdbc:postgresql://postgres:5432/streamq_db
      - SPRING_DATASOURCE_USERNAME=streamq_user
      - SPRING_DATASOURCE_PASSWORD=streamq_password
    restart: unless-stopped

  postgres:
    image: postgres:15.8
    container_name: streamq-postgres
    ports:
      - "5432:5432"
    environment:
      POSTGRES_DB: streamq_db
      POSTGRES_USER: streamq_user
      POSTGRES_PASSWORD: streamq_password
    volumes:
      - postgres-data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U streamq_user -d streamq_db"]
      interval: 10s
      timeout: 5s
      retries: 5
    restart: unless-stopped

volumes:
  postgres-data:
